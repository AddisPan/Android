<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a108222040_listview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.A108222040_ListView">
        <activity android:name=".CustomerActivityUpdate"></activity>
        <activity android:name=".SimpleListActivityUpdate" />
        <activity android:name=".SpinnerActivity" />
        <activity android:name=".CustomerActivity" />
        <activity android:name=".SimpleListActivity2" />
        <activity android:name=".SimpleListActivity" />
        <activity android:name=".StrinArrayActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>